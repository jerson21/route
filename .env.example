# ===========================================
# Route Optimizer - Environment Variables
# ===========================================

# ----- Database -----
# Para desarrollo local
DATABASE_URL="postgresql://route_user:route_password@localhost:5432/route_db"

# Para Docker Compose (usa el nombre del servicio)
# DATABASE_URL="postgresql://route_user:route_password@postgres:5432/route_db"

# Credenciales individuales (para docker-compose.prod.yml)
DB_USER=route_user
DB_PASSWORD=route_password
DB_NAME=route_db

# ----- JWT Secrets -----
# IMPORTANTE: Generar claves seguras para produccion
# Ejecutar: openssl rand -hex 32
JWT_ACCESS_SECRET=change-this-secret-key-in-production-min-32-chars
JWT_REFRESH_SECRET=change-this-refresh-secret-key-in-production-min-32-chars

# ----- Google Maps API -----
# Requerido para geocodificacion y optimizacion de rutas
# Habilitar: Geocoding API, Directions API, Distance Matrix API, Maps JavaScript API, Places API
GOOGLE_MAPS_API_KEY=AIzaSy...

# ----- App Configuration -----
NODE_ENV=development
PORT=3001
FRONTEND_URL=http://localhost:5173

# ----- Production (SSL con Traefik) -----
# Descomenta estas lineas para produccion con docker-compose.prod.yml
# DOMAIN=rutas.tuempresa.com
# ACME_EMAIL=admin@tuempresa.com

# ----- Redis (Opcional) -----
# REDIS_URL=redis://localhost:6379
